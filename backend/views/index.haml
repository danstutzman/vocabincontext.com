%html
  %head
    %title Index
    %link(href="/css/application.css" rel="stylesheet" type="text/css")
  %body
    %h2 Instructions
    %ol
      %li
        Click a word below:
        %br
        %ul(id='words')
        %br(clear='both')
      %li
        Click one of the song lyrics that appears:
        %br
        %ul(id='lyrics')
      %li While it plays, listen for that word
    != haml :_loading
    != haml :_js
    :javascript
      require(["cs!app/app"], function(app) { main.setupFromRequestParams(); });
