%h1 Vocab in Context
%form#query-form(method='post' action='/')
  - if params['query'].nil?
    #search-prompt Enter a Spanish word to hear it in context.
    %input.query-input{ :type => 'text', :name => 'query',
                        :value => params['query'] }
    %input#query-submit(type='submit' value='Search')
  - else
    %p
      The word
      %input.query-input.inline{ :type => 'text', :name => 'query',
                                 :value => params['query'] }
      was found in the following songs:

- if @excerpts.nil?
  -# no query was entered yet
- elsif @excerpts.size == 0
  No results for this query.
- else
  %table#excerpts-list
    %tr
      %th.action
      %th.lyric Line of the song
      %th.artist-name Artist
      %th.song-name Song title
    - @excerpts.each do |excerpt|
      %tr
        %td.action
          - if excerpt[:alignment]
            %a{ :href => "/split_mp3s/#{excerpt[:youtube_video_id]}.#{excerpt[:alignment].start_centis}.#{excerpt[:alignment].finish_centis}.mp3", :target => '_blank' }
              %button Play
          - else
            %button Label
        %td.lyric= "\u201c#{excerpt[:line]}\u201d"
        %td.artist-name= excerpt[:artist_name]
        %td.song-name
          %a{ :href => "/song/#{excerpt[:song_id]}" }
            =excerpt[:song_name]

  %hr
  %a{ :href => "/search?query=#{params['query']}&offset=#{params['offset'].to_i + FerretSearch::MAX_NUM_EXCERPTS_TO_RETURN}" }
    Next page
